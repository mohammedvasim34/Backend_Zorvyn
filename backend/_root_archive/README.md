# Backend_Zorvyn

Project markdown consolidated into this single file.

## Apps
- backend: FastAPI service
- frontend: React + Vite client

## Run
- Backend: from `backend/`, run `uvicorn main:app --reload`
- Frontend: from `frontend/`, run `npm run dev`
